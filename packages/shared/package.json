{
  "name": "@app/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and DTOs for @app/api and @app/web.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo \"@app/shared: no dev step\" && exit 0",
    "build": "echo \"@app/shared: consumed as TS source — no build step\" && exit 0",
    "test": "echo \"@app/shared: no tests yet\" && exit 0",
    "lint": "eslint src --ext .ts"
  }
}
