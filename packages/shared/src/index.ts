/**
 * @app/shared — shared TypeScript types and DTOs across @app/api and @app/web.
 *
 * Real campaign / recipient / auth DTOs land in F2 (auth) and F3 (campaigns).
 * Keeping a placeholder export so the package resolves in F1.
 */
export {};
