# apps/web environment — copy to apps/web/.env.local for local overrides.
# All vars MUST be prefixed with VITE_ to be exposed to the browser bundle.
#
# Optional: api.ts falls back to http://localhost:4000 if this is not set.
VITE_API_BASE_URL=http://localhost:4000
